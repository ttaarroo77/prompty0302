<div class="row">
  <div class="col-md-8">
    <h1>プロンプト一覧</h1>
    
    <% if @prompts.any? %>
      <div class="row">
        <% @prompts.each do |prompt| %>
          <div class="col-md-6 mb-4">
            <div class="card h-100">
              <div class="card-body">
                <h5 class="card-title"><%= prompt.title %></h5>
                <p class="card-text text-truncate"><%= prompt.description %></p>
                <div class="mb-2">
                  <% prompt.tags.each do |tag| %>
                    <span class="badge bg-secondary me-1"><%= tag.name %></span>
                  <% end %>
                </div>
                <%= link_to "詳細", prompt_path(prompt), class: "btn btn-primary btn-sm" %>
              </div>
            </div>
          </div>
        <% end %>
      </div>
    <% else %>
      <p>プロンプトがまだありません。新しいプロンプトを追加してください。</p>
    <% end %>
  </div>
  
  <div class="col-md-4">
    <div class="card mb-4">
      <div class="card-header">
        新しいプロンプト
      </div>
      <div class="card-body">
        <%= form_with(model: @prompt, local: true) do |f| %>
          <% if @prompt.errors.any? %>
            <div class="alert alert-danger">
              <ul class="mb-0">
                <% @prompt.errors.full_messages.each do |message| %>
                  <li><%= message %></li>
                <% end %>
              </ul>
            </div>
          <% end %>
          
          <div class="mb-3">
            <%= f.label :title, "タイトル", class: "form-label" %>
            <%= f.text_field :title, class: "form-control" %>
          </div>
          
          <div class="mb-3">
            <%= f.label :url, "URL", class: "form-label" %>
            <%= f.url_field :url, class: "form-control" %>
          </div>
          
          <div class="mb-3">
            <%= f.label :description, "説明", class: "form-label" %>
            <%= f.text_area :description, rows: 5, class: "form-control" %>
          </div>
          
          <%= f.submit "追加する", class: "btn btn-primary w-100" %>
        <% end %>
      </div>
    </div>
    
    <div class="card">
      <div class="card-header d-flex justify-content-between align-items-center">
        <span>既存タグ一覧</span>
        <span class="badge bg-info"><%= @standalone_tags.count %>タグ</span>
      </div>
      <div class="card-body">
        <div class="mb-3">
          <input type="text" id="tagSearch" class="form-control form-control-sm" placeholder="タグを検索...">
        </div>
        <div class="tag-cloud" style="max-height: 300px; overflow-y: auto;">
          <% @standalone_tags.each do |tag| %>
            <span class="badge bg-light text-dark me-1 mb-1 tag-item"><%= tag.name %></span>
          <% end %>
        </div>
      </div>
    </div>
  </div>
</div>

<script>
  document.addEventListener('DOMContentLoaded', function() {
    const searchInput = document.getElementById('tagSearch');
    const tagItems = document.querySelectorAll('.tag-item');
    
    searchInput.addEventListener('input', function() {
      const searchValue = this.value.toLowerCase();
      
      tagItems.forEach(function(item) {
        const tagName = item.textContent.toLowerCase();
        if (tagName.includes(searchValue)) {
          item.style.display = 'inline-block';
        } else {
          item.style.display = 'none';
        }
      });
    });
  });
</script>
